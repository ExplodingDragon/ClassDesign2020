package i.design.modules.token.models

import org.junit.jupiter.api.Test

internal class TokenHeaderTest {
    @Test
    fun testAll() {
        println(TokenHeader(121, 121).toString())
        println(TokenHeader(121, 121).toString())
        println(TokenHeader(121, 121).toString())
    }
}